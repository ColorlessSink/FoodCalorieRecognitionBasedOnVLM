# 用户待完成清单

> 本清单是大作业中"代码/实验/文档侧已尽力、但需要真人操作或额外资源"的事项。
> 按优先级排序：P0 = 不做会影响验收，P1 = 显著加分，P2 = 有余力再做。
> 每项都写清了"为什么做不了 / 怎么做 / 大概花多久"。

---

## P0 · 必做（影响验收）

### 1. 自采真实食物照片 ≥100 张（数据集"真实图像"要求）

- **现状**：当前 600 张测试图全部来自 ChineseFoodNet（网络图），混合盘 120 张是程序合成的。作业数据集要求"每张图像需包含……自采集或公开数据集均可"，纯公开数据集字面上合规，但答辩时"自己拍过吗"几乎必被问。
- **怎么做**：手机拍三餐，单食物 + 混合餐盘都拍，3 种场景（餐厅灯光/家庭日光/暗光或俯拍斜拍）各覆盖一些，存到 `dataset_50cls/selfshot/`，文件名按 `菜名_序号.jpg`。100 张大约是一周吃饭的量。
- **拍完可顺手做**：挑 20-30 张跑 `python demo/web_demo.py` 截图存档（真实图上的表现是答辩好素材）；如要量化，可把自采图按 50 类内的类别挑出来跑一次识别，报告真实域泛化。

### 2. GitHub 上传与提交

- **现状**：代码在本地 `D:\study\University\Course\EagleLab\小作业\大作业\`，尚未推到远程仓库。
- **怎么做**：
  1. 在 GitHub 建私有仓库（作业建议 private）；
  2. 检查 `.gitignore` 已忽略 `ChineseFood Net 3/`、`dataset_20cls/`、`dataset_50cls/`、`.claude/`（README 已声明）——**注意 `results/figures/` 与 `results/*.json` 要入库**，它们是报告数字的凭证；
  3. `git add . && git commit -m "大作业完整交付" && git push`。
- **顺带确认**：`git status` 干净、无 `_LOCAL_SNAP` 本机绝对路径硬编码泄漏（各脚本换机器时要改，README 已说明）。

### 3. 答辩 PPT / 演示准备

- **建议结构**（10 分钟版）：
  1. 问题与约束（1 页：无分量标注 + LLM 视觉不可用 → 三层解耦架构图）；
  2. 数据集（1 页：50 类 1500/300/600 + 混合盘 120 + 合成真值口径声明——主动讲，别等被问）;
  3. 方法（2 页：四模块流水线图 + 分量 v1 失败 → v2 相对调制的诊断过程，这是最出彩的"工程叙事"）;
  4. 实验（2 页：达标汇总表 + fig1/fig3/fig6 三张图 + 混合盘分层表）；
  5. 演示（2 分钟：`python demo/web_demo.py --no-llm` 现场跑单食物 + 混合盘 + "再来一份"共指）；
  6. 失败案例与改进（1 页：面点家族混淆 + "自信地错"）。
- **演示预案**：提前把 web_demo 起好、图片放桌面；断网用 `--no-llm` 规则模式保底（流水线不依赖网络是设计卖点）。
- **必背数字**：Top-1 78.33/81.50/83.67；分量 MAE 全量 24-25g；混合盘 oracle 63.9 / e2e 120.9；14 用例 34 轮 100%。

### 4. GPT-4V / 其他多模态 API 基线实测（"≥2 种 VLM 基线"的补强）

- **现状**：作业要求"至少对比 2 种 VLM 基线（如 CLIP vs. LLaVA，或 CLIP vs. GPT-4V API）"。当前五种基线（3 模板零样本 + 少样本 + LoRA）全部基于 Chinese-CLIP，属于"同模型三模式"而非"两种 VLM"；报告 5.2-⑦ 已如实声明局限。
- **怎么做（任选其一，约 1-2 小时）**：
  - **方案 A（最省事）**：有 GPT-4V/Claude/Gemini API key 的话，抽 50-100 张测试图写一个 20 行的调用脚本（图 + "这是哪道中餐？只回答菜名"），算 Top-1 与 Chinese-CLIP 对比，结果追加进 `results/recognition_baseline.json` 与报告 4.3.1；
  - **方案 B（免费）**：在 HuggingFace 上拉一个小型开源 VLM（如 Qwen2-VL-2B / MiniCPM-V）在服务器 3090 上推理同一批图。服务器可用（`ssh wangmingxiang2025@10.130.128.81`，注意别动别人的进程）；
  - **方案 C（零成本兜底）**：不实测，答辩口径就按报告写的——"受代理 LLM 视觉不可用约束，基线在 CLIP 范式内做了三种模式 + 3 种模板的对比；跨模型 VLM 对比是 future work"。这是诚实的说法，风险是遇到严格的验收老师。
- **推荐**：至少做方案 A 或 B 之一，把这条从"局限"变成"已补"。

### 5. 确认最终数字与文件一致性（交前检查）

- 交前把 `README.md`、`report/final_report.md` §4.6、`report/process_log.md` §3.8 三处指标汇总表对一遍（现已统一为：14 用例 34 轮、文献综述 6019 字 40 引、混合盘 oracle 63.9/e2e 120.9）。
- 确认 `results/` 下 JSON 与表格数字一致（尤其 `mixed_plate_eval.json`、`dialogue_test_results.json` 14 用例）。
- 若老师要求 PDF/Word 版报告，把四个 md 导出（Typora/VS Code 插件均可）。

---

## P1 · 显著加分

### 6. 混合餐盘端到端 MAE 120.9 → ≤100（唯一未硬达标指标）

- **现状**：oracle 63.9 达标、e2e 120.9 超 20.9。分层定位显示瓶颈在组件识别（51/680 错菜），不在检测（召回 100%）或分量。
- **最短路径**（预计半天）：
  1. 把组件识别从 LoRA 模式切换到**少样本 10-shot 模式**（单食物上它比 LoRA 高 2.2 点），改 `experiments/mixed_eval.py` 里 `rec.mode` 重跑 120 盘——如果组件 Top-1 从 82.4% 升到 ~85%，e2e MAE 有望落到 ~105 左右；
  2. 若还不够，对组件裁剪图做**轻度增强推理**（如 padding 后再送识别，缓解贴片边缘伪影）；
  3. 若还不达标，接受现状并在答辩主动讲分层分析——"oracle 达标证明流水线健康，e2e 差距全部来自识别，改进路径明确"本身是加分的工程判断。
- 服务器上跑：`cd ~/eaglelab && python3 experiments/mixed_eval.py`（约 10 分钟，3090）。

### 7. 自采图像的量化评估（若完成 P0-1）

- 把自采图中能对上 50 类的挑出来（哪怕 30 张），跑一遍识别 + 分量，写进报告 4.3.6"真实域测试"——这是"真实图像"要求的最有力证据，也是混合盘合成图域差的对照。

### 8. Web Demo 公网可访问（答辩远程演示备用）

- 若答辩教室网络不便本地起服务：在服务器上 `python3 demo/web_demo.py --share`（gradio 公网链接 72 小时有效），或提前录屏 2 分钟作保底。
- 注意：服务器是多人共享，用 `--port 7863` 之类空闲端口，且 `export GRADIO_TEMP_DIR=$HOME/gradio_tmp`（/tmp/gradio 属别的用户会 PermissionError）。

---

## P2 · 有余力再做

### 9. 置信度门控升级（top1-top2 margin 双判据）

- 失败分析发现 60% 的识别错误 conf>0.9（"自信地错"）。把门控条件从 `conf < max(0.3, target_top1×0.5)` 升级为 `conf < τ1 OR (conf−top2_sim) < τ2`，在 600 张测试上扫 τ2，报告拦截率/误拦率曲线。约 2 小时，直接呼应失败案例分析的"改进方向 5"。

### 10. 移动端/推理提速

- 当前 GPU 批处理 600 张约 9s（吞吐尚可），单张交互 ~0.3s。若想展示移动可行性：torch.onnx 导出 + INT8 量化，跑个 benchmark 写进报告。约半天，非必需。

### 11. 文献综述可选增补

- 已达标（6019 字 / 40 引 / 近 3 年 50%+ / 顶会顶刊 9 篇 / 5 方向）。有余力可再补 2-3 篇 2025-2026 的最新工作（如多模态膳食记录 agent 方向），把"近 3 年"占比再抬一抬。非必需。

---

## 附：已完成事项速查（不需要再做）

| 事项 | 状态 |
|------|------|
| 文献综述（6019 字 / 40 引 / 5 方向 / 顶会顶刊 9 篇） | ✅ `report/literature_review.md` |
| 50 类数据集（1500/300/600 + 场景分层 + 合成真值） | ✅ `dataset_50cls/` |
| 混合餐盘数据（120 盘）+ 检测 + 分层评估 + 对话用例 | ✅ `data/build_mixed_plates.py`、`models/mixed_detector.py`、`experiments/mixed_eval.py` |
| 四模块算法 + 3 创新点 + 3 组消融 | ✅ `models/`、`results/ablation_study.json` |
| 全部硬指标（除混合盘 e2e）| ✅ 见各报告汇总表 |
| 失败案例 18 例 | ✅ `report/failure_cases.md` |
| 总结报告（8203 汉字，含表格代码） | ✅ `report/final_report.md` |
| requirements.txt 双环境版本固定 | ✅ 根目录 |
| config.yaml 集中配置 | ✅ `config/config.yaml` |
| README（含硬件说明、目录结构、单条命令运行） | ✅ `README.md` |
| CLI Demo + Web Demo（Gradio 6，服务器 GPU 实测通过） | ✅ `demo/cli_demo.py`、`demo/web_demo.py` |
| 对话用例 14 个 34 轮（含混合盘 3 例） | ✅ `demo/dialogue_cases.json`、`results/dialogue_test_results.json` |
