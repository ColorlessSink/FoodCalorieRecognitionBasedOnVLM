# =============================================================================
# 大作业·卡路里识别智能体 依赖（版本固定）
# =============================================================================
# 为什么固定版本：作业第五部分要求 requirements.txt 固定版本号，且本仓库
# 在两套环境实测通过——本地 Windows（RTX 4050）与远程服务器（RTX 3090）。
# 两套环境因 CUDA/Python 版本不同，torch/transformers/peft 版本不同，
# 代码里对 transformers 4.x/5.x 的特征接口差异已做三路兼容
# （见 models/food_recognizer.py encode_texts/encode_images）。
#
# ── 环境 A：本地 Windows 11 + Python 3.13 + RTX 4050 Laptop 6GB（实验主环境）──
#   torch==2.13.0+cu130        # pip install torch --index-url https://download.pytorch.org/whl/cu130
#   transformers==5.14.1
#   peft==0.19.1
#   huggingface-hub>=1.5,<2.0  # transformers 5.x 的硬约束（<1.0 会让它崩）
#   gradio==6.26.0            # 需要 hub>=1.x；4.x 的 gradio 与 hub 1.x 冲突（HfFolder 已删）
#   accelerate==1.14.0
#   datasets==5.0.0
#   pandas==2.3.3
#   numpy==2.3.2
#   Pillow==10.4.0
#   opencv-python==5.0.0.93
#   scikit-learn==1.9.0
#   matplotlib==3.10.5
#   seaborn==0.13.2
#   openpyxl==3.1.5
#   requests==2.28.2
#   pyyaml==6.0.3
#   segment-anything==1.0   # SAM ViT-B 混合盘检测后端（models/sam_detector.py，
#                           #   权重 models/sam_vit_b_01ec64.pth 375MB 不入库需另放；
#                           #   不装则该后端不可用，其余流程与纹理后端不受影响）
#
# ── 环境 B：远程服务器 Ubuntu 22.04 + Python 3.10 + RTX 3090 24GB ──
#   torch==2.5.1+cu121         # pip install torch --index-url https://download.pytorch.org/whl/cu121
#   transformers==4.46.3       # 与 peft 0.20.0 配套
#   peft==0.20.0
#   huggingface-hub>=0.25,<1.0 # transformers 4.46 的硬约束（>=1.0 会 ImportError）
#   gradio==6.26.0            # 注意：gradio 6.26 声明要 hub>=1.16，但实测与 hub 0.36 共存可用（仅 pip 元数据告警）
#   accelerate==1.14.0
#   datasets==5.0.1
#   pandas==2.3.3
#   numpy==2.2.6
#   Pillow==12.3.0
#   opencv-python==4.10.0.84   # 服务器实测
#   scikit-learn==1.7.2
#   matplotlib==3.9.x          # 服务器实测（与本地 3.10 均可）
#   seaborn==0.13.2
#   openpyxl==3.1.5
#   requests==2.34.2
#   pyyaml==5.4.1
#   segment-anything==1.0   # 同上（SAM 后端可选；服务器未实测 SAM，复现时如需真实混合菜重标再装）
#
# 下面给出默认安装行（环境 A，本地）。若在服务器复现，请按上面环境 B 的
# 版本调整 torch/transformers/peft/hub 四个关键包。
# =============================================================================

torch==2.13.0
transformers==5.14.1
peft==0.19.1
accelerate==1.14.0
datasets==5.0.0
pandas==2.3.3
numpy==2.3.2
Pillow==10.4.0
opencv-python==5.0.0.93
scikit-learn==1.9.0
matplotlib==3.10.5
seaborn==0.13.2
openpyxl==3.1.5
requests==2.28.2
pyyaml==6.0.3
gradio==6.26.0
huggingface-hub>=1.5,<2.0
segment-anything==1.0
